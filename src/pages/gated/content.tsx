import React from 'react';

const Content = () => {
    return (
        <div>
            <p>You can only see this content if you are logged in.</p>
        </div>
    );
};

export default Content;
